<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="drawerArrowStyle">@style/DrawerArrowStyleLight</item>
        <item name="android:fontFamily">@font/roboto_slab</item>
        <item name="toolbarStyle">@style/AppTheme.Toolbar</item>
        <item name="android:colorBackground">@color/whitesmoke</item>
    </style>

    <!-- Theme for Dark Mode-->
    <style name="DarkMode" parent="ThemeOverlay.AppCompat.Dark">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="drawerArrowStyle">@style/DrawerArrowStyleDark</item>
        <item name="android:fontFamily">@font/roboto_slab</item>
        <item name="toolbarStyle">@style/DarkMode.Toolbar</item>
    </style>

    <!-- Whitesmoke Toolbar for AppTheme -->
    <style name="AppTheme.Toolbar" parent="Widget.AppCompat.Toolbar">
        <item name="android:background">@color/whitesmoke</item>
    </style>

    <!-- Black Toolbar for DarkMode -->
    <style name="DarkMode.Toolbar" parent="Widget.AppCompat.Toolbar">
        <item name="android:background">@android:color/black</item>
    </style>

    <!--    Adjust the DrawerArrow Color when Theme is Light. -->
    <style name="DrawerArrowStyleLight" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="color">@android:color/black</item>
        <item name="spinBars">true</item>
    </style>

    <!--    Adjust the DrawerArrow Color when Theme is Dark. -->
    <style name="DrawerArrowStyleDark" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="color">@android:color/white</item>
        <item name="spinBars">true</item>
    </style>

</resources>
